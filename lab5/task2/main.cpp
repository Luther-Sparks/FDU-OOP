#include "../display/display.h"
#include "bird.h"

int main() {
    Display display;
    Bird bird(0,0,0);
    // while (true) {
    //     int c = display.get_char(25);
    //     display.put_string(display.get_cols() / 2, display.get_rows() / 2,
    //                        std::to_string(c));
    // }
}
